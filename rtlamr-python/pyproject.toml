[project]
name = "python-rtlamr"
version = "0.1.0"
description = "Python implementation of RTLAMR-go"
dependencies = [
  "numpy>=1.24",
]

[project.optional-dependencies]
hardware = [
  "pyrtlsdr",
  "pyusb>=1.3.1",
]
dev = [
  "pytest",
]

[tool.black]
line-length = 80